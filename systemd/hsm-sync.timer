[Unit]
Description=HSM Sync timer — run every 15 minutes
Requires=hsm-sync.service

[Timer]
# Adjust OnCalendar to change the sync interval
OnCalendar=*:0/15
Persistent=true

[Install]
WantedBy=timers.target
